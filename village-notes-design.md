# 集落ひとこと特徴（village notes）設計書

作成: 2026-07-29 / ステータス: 設計・情報収集中（実装前）

## 目的
正解時に集落名の下へ1行の特徴（豆知識）を表示し、位置当てゲームに「島の再発見」の価値を足す。
SPEC.md将来拡張「集落ごとの豆知識ポップアップ」の具体化。

## 表示仕様（Codexクロスチェック反映済み — 最終ビジュアルはClaude Design側で決定）
- **場所**: 正解時の情報トースト（現行: 名前・よみ・町名バッジ）に特徴行を追加
- 特徴がある集落のみ表示。**無い集落は名前・よみ・町名を同じ配置のまま特徴欄だけ詰める**（「情報なし」とは絶対に出さない。行ごと消すとバグに見えるためレイアウトは崩さない）
- **表示時間: 特徴あり時は4〜6秒**（現行2.2秒では読めない。Material DesignのSnackbar指針も最低4秒）。特徴なし時は現行2.2秒のまま
- 代替案（Design側の判断で可）: トーストを次の正解まで残す「結果カード」方式
- **文字数: 最大2行・35字上限**（430px幅・フォント13px想定で1行28〜31字。`line-clamp: 2`。13px未満に縮小しない）
- アクセシビリティ: トーストは `role="status"`（aria-live: polite相当）でフォーカスを奪わない
- 地図ラベルの下に常時表示はしない（57集落で渋滞するため不採用）

### Claude Design側で決めてほしいこと
1. トースト内の特徴テキストの組み方（行送り・文字サイズ・色。現行トーストは `#toast` 参照）
2. 表示時間4〜6秒か「次の正解まで残す」方式か（連続正解時の折り重なり挙動含む）
3. クリア画面や「図鑑モード」への発展はスコープ外（今回はトーストのみ）

## データ形式
`data.js` の各集落に `note` を追加（無い集落は省略 = undefined）:
```js
{ id: "meteku", name: "目手久", kana: "めてぐ", town: "isen", x: 61.8, y: 140.1,
  note: "島内最大の観光闘牛場がある闘牛どころ" },
```
- 文字数: 20〜35字目安（スマホ幅430px・1〜2行に収まる）
- 出典は `village-notes-sources.md`（本リポジトリ）にID毎に記録。data.jsには表示文のみ

## 記載ポリシー（重要）
1. **出典が取れないことは書かない（捏造禁止）**。公式サイト・自治体資料・信頼できる百科事典を出典とする
2. ネタのフォールバック階層: 名所・施設 → 歴史・行事 → 地理的特徴 → 地名・成り立ち
3. それでも書けない集落は `note` なしを許容（ゲームは通常表示にフォールバック）
4. 存命・実在の個人名は歴史的・広く公知の人物のみ（例: 泉重千代翁）。政治家ネタは使わない

## 情報収集の状況（2026-07-29 完了）
| 町 | note確定 | null |
|---|---|---|
| 伊仙町 | 20/20 | — |
| 徳之島町 | 24/26 | 内千川・山里（公式紹介も空欄・信頼出典なし） |
| 天城町 | 11/11 | — |

- 一次情報: 徳之島町公式30集落紹介 / 伊仙町公式集落紹介 / 各町公式・県資料
- Codex（Web検索ON）でクロスチェック済み。主要差し替え（朝潮太郎・当部・白井神社）は一次資料で二次検証済み
- **確定ドラフトと出典: `village-notes-draft.md`**（注記・落としたネタ・ユーザー確認事項も同ファイル）

## 実装手順（設計確定後）
1. ユーザーがドラフト文面を確認（特に目手久ら地元集落と、null 2件の扱い）
2. noteを `data.js` に追加（出典はvillage-notes-draft.mdが正）
3. Claude Design側でトーストのデザイン調整（Designプロジェクト 56c53faf-… の dist/ 出力を再取得してシンク）
4. 本番反映・実機確認
